<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yuyuan.androidjava_peluche">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/panda_icone"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/panda_icone_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".NewMessageActivity"></activity>
        <activity android:name=".ForumMessagesActivity" />
        <activity android:name=".NewTopicActivity" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".DeconnexionActivity" />
        <activity
            android:name=".MonCompteScrollingActivity"
            android:label="@string/title_activity_mon_compte_scrolling"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name=".InscriptionActivity" />
        <activity android:name=".ConnexionActivity" />
        <activity android:name=".BoutiqueActivity" />
        <activity android:name=".ComptinesListeActivity" />
        <activity android:name=".AproposActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBa8gXeHhta-pDTJTyPojgRHeM2cUFW5HE" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".ContactScrollingActivity"
            android:label="@string/title_activity_contact_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>